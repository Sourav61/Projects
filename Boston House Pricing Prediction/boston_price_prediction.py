# -*- coding: utf-8 -*-
"""Boston Price Prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/github/Sourav61/Goeduhub-Assignments/blob/main/Boston_Price_Prediction.ipynb
"""

from google.colab import drive
drive.mount('/content/drive')

import numpy as np
import pandas as pd

import matplotlib.pyplot as plt

import sklearn
from sklearn.linear_model import LinearRegression

from sklearn.datasets import load_boston

boston = load_boston()
boston

boston.keys()

boston['data'].shape

df = pd.DataFrame(boston.data)

df.head(10)

df.tail(10)

print(boston.feature_names)

print(boston.DESCR)

print(boston.target)

df.columns = boston.feature_names
df

df['Price'] = boston.target

df

y = df['Price']

x = df.drop('Price',axis=1)

x.head()

y.head()

from sklearn.model_selection import train_test_split

xtrain, xtest, ytrain, ytest = train_test_split(x, y, test_size = 0.2, random_state = 5)

lm = LinearRegression()
lm.fit(xtrain, ytrain)

y_pred_train = lm.predict(xtrain)
y_pred_test = lm.predict(xtest)

df = pd.DataFrame(y_pred_test, ytest)
df.head(10)

from sklearn.metrics import mean_squared_error, mean_absolute_error
print("The Mean Squared Error is: ", end=" ")
mse = mean_squared_error(ytest, y_pred_test)
print(mse)
print("The Mean Absolute Error is: ", end=" ")
mae = mean_absolute_error(ytest, y_pred_test)
print(mae)

plt.scatter(ytest, y_pred_test,c='m', marker="^")
plt.plot(ytest, lm.predict(xtest))
plt.show()